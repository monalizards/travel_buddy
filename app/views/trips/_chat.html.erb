<h3>Chat panel</h3>
<ul>
  <% @trip.messages.each do |message| %>
    <li><%= render "message", message: message %></li>
  <% end %>
</ul>

<%= simple_form_for(message) do |f| %>
  <%= f.hidden_field :trip_id, value: @trip.id %>
  <%= f.input :message %>
  <%= f.button :submit %>
<% end %>
